{% extends 'base.html.twig' %}

{% block title %}Gestion des certifications{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    {% include 'bulletin/_styles.html.twig' %}
{% endblock %}

{% block header_content %}
    <div class="admin-page-header">
        <h1>Gestion des certifications</h1>
        <p class="text-muted">Délivrez et suivez les certifications basées sur les bulletins validés.</p>
    </div>
{% endblock %}

{% block body %}
    <div class="admin-page">
        <div class="admin-card">
            <div class="admin-card-header">
                <div>
                    <h2 class="admin-card-title">Liste des certifications</h2>
                    <p class="admin-card-subtitle">Certifications académiques générées</p>
                </div>
                <a href="{{ path('app_certification_new') }}" class="btn-primary">
                    + Nouvelle certification
                </a>
            </div>

            <div class="admin-card-body">
                {% if certifications is empty %}
                    <p class="text-muted mb-0">Aucune certification n’a encore été générée.</p>
                {% else %}
                    <div class="admin-table-wrapper">
                        <table class="admin-table">
                            <thead>
                            <tr>
                                <th>ID</th>
                                <th>Étudiant</th>
                                <th>Bulletin</th>
                                <th>Type</th>
                                <th>Code</th>
                                <th>Statut</th>
                                <th>Émise le</th>
                                <th class="text-end">Actions</th>
                            </tr>
                            </thead>
                            <tbody>
                            {% for certification in certifications %}
                                <tr>
                                    <td>#{{ certification.id }}</td>
                                    <td>{{ certification.student ? certification.student.email : '—' }}</td>
                                    <td>
                                        {% if certification.bulletin %}
                                            Bulletin #{{ certification.bulletin.id }}
                                        {% else %}
                                            —
                                        {% endif %}
                                    </td>
                                    <td>{{ certification.type }}</td>
                                    <td><code>{{ certification.verificationCode }}</code></td>
                                    <td>
                                        {% if certification.status == 'ACTIVE' %}
                                            <span class="badge-soft badge-green">Active</span>
                                        {% else %}
                                            <span class="badge-soft badge-gray">{{ certification.status }}</span>
                                        {% endif %}
                                    </td>
                                    <td>{{ certification.issuedAt ? certification.issuedAt|date('d/m/Y H:i') : '' }}</td>
                                    <td class="text-end">
                                        <a href="{{ path('app_certification_show', {id: certification.id}) }}" class="btn-icon">
                                            👁
                                        </a>
                                        <a href="{{ path('app_certification_edit', {id: certification.id}) }}" class="btn-icon">
                                            ✏️
                                        </a>
                                        {{ include('certification/_delete_form.html.twig', {certification: certification}) }}
                                    </td>
                                </tr>
                            {% endfor %}
                            </tbody>
                        </table>
                    </div>
                {% endif %}
            </div>
        </div>
    </div>
{% endblock %}