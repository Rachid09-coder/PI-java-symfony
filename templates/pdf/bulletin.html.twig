<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body { font-family: 'DejaVu Sans', sans-serif; font-size: 12px; color: #333; margin: 0; padding: 0; }
        .container { width: 100%; padding: 30px; }
        .header { border-bottom: 2px solid #6366F1; padding-bottom: 10px; margin-bottom: 20px; }
        .logo { width: 100px; height: auto; float: left; }
        .school-info { float: left; margin-left: 20px; }
        .doc-title { text-align: center; font-size: 18px; font-weight: bold; margin: 20px 0; clear: both; text-transform: uppercase; }
        .student-info { margin-bottom: 20px; border: 1px solid #ddd; padding: 15px; background: #f9f9f9; }
        table { width: 100%; border-collapse: collapse; margin-bottom: 20px; }
        th, td { border: 1px solid #ddd; padding: 10px; text-align: left; }
        th { background-color: #6366F1; color: white; }
        .footer { position: fixed; bottom: 30px; width: 100%; font-size: 10px; color: #777; border-top: 1px solid #ddd; padding-top: 10px; }
        .qrcode { width: 80px; height: 80px; float: right; }
        .signature-section { margin-top: 40px; }
        .signature-box { width: 30%; float: left; text-align: center; }
        .sig-image { width: 100px; height: auto; display: block; margin: 5px auto; }
        .mention { font-weight: bold; font-size: 14px; text-align: right; margin-top: 10px; }
        .clear { clear: both; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            {% if logo %}
                <img src="data:image/png;base64,{{ (projectDir ~ '/public/' ~ logo.imagePath)|file_to_base64 }}" class="logo">
            {% else %}
                <div style="float:left; font-size: 24px; font-weight: 800; color: #6366F1;">EduSmart</div>
            {% endif %}
            <div class="school-info">
                <strong>EduSmart Academy</strong><br>
                123 Rue de l'Éducation, Tunis<br>
                Email: contact@edusmart.tn
            </div>
            <img src="data:image/png;base64,{{ qrBase64 }}" class="qrcode">
            <div class="clear"></div>
        </div>

        <div class="doc-title">Bulletin de Notes</div>

        <div class="student-info">
            <strong>Nom:</strong> {{ bulletin.student.name }}<br>
            <strong>Prénom:</strong> {{ bulletin.student.prenom }}<br>
            <strong>ID Étudiant:</strong> #{{ bulletin.student.id }}<br>
            <strong>Période:</strong> {{ bulletin.academicYear }} - {{ bulletin.semester }}
        </div>

        <table>
            <thead>
                <tr>
                    <th>Module / Matière</th>
                    <th style="text-align: center;">Coef</th>
                    <th style="text-align: center;">Note / 20</th>
                    <th>Appréciation</th>
                </tr>
            </thead>
            <tbody>
                {% for line in bulletin.reportCardLines %}
                <tr>
                    <td>{{ line.moduleName }}</td>
                    <td style="text-align: center;">{{ line.coefficient }}</td>
                    <td style="text-align: center;">{{ line.note }}</td>
                    <td>{{ line.teacherComment }}</td>
                </tr>
                {% endfor %}
            </tbody>
            <tfoot>
                <tr style="background: #f2f2f2; font-weight: bold;">
                    <td colspan="2" style="text-align: right;">MOYENNE GÉNÉRALE</td>
                    <td style="text-align: center;">{{ bulletin.average|number_format(2) }}</td>
                    <td></td>
                </tr>
            </tfoot>
        </table>

        <div class="mention">
            Mention: {{ bulletin.computeMention() }} 
            {% if bulletin.classRank %}| Rang: {{ bulletin.classRank }}{% endif %}
        </div>

        <div class="signature-section">
            <div class="signature-box">
                Le Chef de Département<br>
                {% if cachet %}
                    <img src="data:image/png;base64,{{ (projectDir ~ '/public/' ~ cachet.imagePath)|file_to_base64 }}" class="sig-image">
                {% endif %}
            </div>
            <div class="signature-box" style="float: right;">
                Le Directeur Général<br>
                {% if signature %}
                    <img src="data:image/png;base64,{{ (projectDir ~ '/public/' ~ signature.imagePath)|file_to_base64 }}" class="sig-image">
                {% endif %}
            </div>
            <div class="clear"></div>
        </div>

        <div class="footer">
            Document authentifié. Code de vérification: {{ bulletin.verificationCode }}<br>
            Vérifiez l'authenticité sur: {{ verifyUrl }}
        </div>
    </div>
</body>
</html>
