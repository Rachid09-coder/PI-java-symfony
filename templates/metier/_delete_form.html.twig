<form method="post" action="{{ path('app_metier_delete', {'id': metier.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce métier ?');" style="display: inline-block;">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ metier.id) }}">
    <button class="btn btn-sm btn-outline-danger">
        <i class="fas fa-trash"></i>
    </button>
</form>
